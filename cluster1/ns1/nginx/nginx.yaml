apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-project
  namespace: ns1
spec:
  replicas: 4
  selector:
    matchLabels:
      app: nginx-app
  template:
    metadata:
      labels:
        app: nginx-app
    spec:
      containers:
      - name: nginx
        image: nginx:1.17.3
        ports:
          - containerPort: 80